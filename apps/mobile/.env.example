EXPO_PUBLIC_API_URL=http://localhost:4000
# Real auth is the default. Leave false. Setting true only has any effect in a dev
# build (__DEV__) and lets you skip the login flow with placeholder data locally.
EXPO_PUBLIC_DEMO_AUTH_BYPASS=false
